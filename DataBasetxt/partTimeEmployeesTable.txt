CREATE TABLE part_time_employees (
    PartTimeEmployeeID int auto_increment,
    UserName varchar(255), 

    PRIMARY KEY (EmployeeID),
	
    FOREIGN KEY (UserName)
      REFERENCES employees(UserName)    

);

alter table part_time_employees auto_increment=1;

INSERT INTO part_time_employees (UserName) VALUES ('Random_Name');

SELECT * FROM part_time_employees;

DELETE FROM part_time_employees WHERE EmployeeID = 2; 

DROP Table part_time_employees;
