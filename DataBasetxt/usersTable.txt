CREATE TABLE users (
    
    Username varchar(255),
    Password varchar(255),
    FirstName varchar(255),
    LastName varchar(255),
    DateOfBirth varchar(255),
    Email varchar(255),
    Mobile varchar(20),
    Address varchar(255),
    City varchar(255),
    State varchar(255),
    ZipCode varchar(5),

    PRIMARY KEY (Username)
    
);


INSERT INTO users (Username, Password, FirstName, LastName, DateOfBirth, Email, Mobile, Address, City, State, ZipCode)
VALUES ('ndodley', 'gamer204', 'Nikhil', 'Dodley', '08/09/1996', 'ndodley@yahoo.com', 'Random_Mobile', 'Random_Add', 'Random_City', 'Random_State', 'Random_ZipCode');

SELECT * FROM users;

DELETE FROM users WHERE Username = '___'; 

DROP Table users;
