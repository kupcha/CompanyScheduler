CREATE TABLE admins (
    AdminID int auto_increment,
    UserName int, 

    PRIMARY KEY (AdminID),
	
    FOREIGN KEY (UserName)
      REFERENCES users(Username)    

);

alter table admins auto_increment=1;

INSERT INTO admins (AdminID) VALUES (1);

SELECT * FROM admins;

DELETE FROM admins WHERE UserName = 2; 

DROP Table admins;
